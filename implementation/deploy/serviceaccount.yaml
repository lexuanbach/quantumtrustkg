# Identity of the controller pod. The ClusterRoleBinding in rbac.yaml grants this
# service account its permissions.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: quantumtrustkg-controller
  namespace: quantumtrustkg-system
